finalize dotfiles: ghostty, hushlogin, tmux auto-start, and cleanup

- ghostty: Catppuccin Mocha palette inline, hidden title bar, paper icon
- ghostty: 12px padding to clear macOS rounded corners
- ghostty: cursor blink on, scrollback off, Cmd+D split disabled
- ghostty: auto-start tmux via $GHOSTTY_RESOURCES_DIR in zsh and bash
- hushlogin: suppress "Last login" on shell open
- aliases: pubip, privip (all private IPs), pubkey, pubkeys
- zsh/bash: /usr/local/bin in PATH for ollama and VSCode ARM CLI
- packages: GUI apps from official sites only, brew for CLI tools only
- all configs: comments normalized to single short lines
- README: concise, consistent markdown throughout

